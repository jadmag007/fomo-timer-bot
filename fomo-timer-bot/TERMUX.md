# Fomo Timer Bot на Android (Termux)

Бот целиком живёт на телефоне: напоминания приходят в тот же Telegram,
страница таймеров открывается в браузере этого же телефона. Компьютер не нужен.

---

## Что понадобится

| Что | Где взять |
|-----|-----------|
| Android 8+ | — |
| **Termux** | **F-Droid** или GitHub Releases. ⚠️ Версия из Play Store устарела и не подходит |
| ~400 МБ места | Python + зависимости готовыми колёсами; запасная сборка с Rust — до 1.5 ГБ |
| Токен бота | уже есть у тебя (из .env на ПК) |

Установка Termux: открой https://f-droid.org/packages/com.termux/ в браузере
телефона → скачай APK → разреши установку из неизвестных источников → ставь.

---

## Установка бота (10 минут)

Открой Termux и выполни по очереди:

```bash
pkg update -y && pkg upgrade -y
pkg install -y git
git clone https://github.com/jadmag007/fomo-timer-bot
cd fomo-timer-bot
bash install.sh
```

`install.sh` сам увидит Termux и сделает всё сам: поставит Python, заберёт
ЗАРАНЕЕ зафиксированную пару pydantic 2.12.5 + pydantic-core 2.41.5
(Rust-ядро aiogram) ГОТОВЫМИ колёсами из зеркала TUR PyPI и доставит
остальные зависимости — без компиляции, обычно 2-5 минут. Пару фиксируем
не случайно: в зеркале есть ядро только версии 2.41.5, а самый свежий
pydantic требует более новое — готовой сборки под андроид нет (так что
если pydantic уже стоит — установщик его не тронет). Если зеркало
недоступно, install.sh сам переключится на запасную сборку через Rust из
репозитория Termux (10-25 минут, разовая). Первая строка установщика
показывает его версию — если после `git pull` там не самая свежая, значит
запускается старый скрипт. Когда спросит токен — вставь его длинным тапом
→ Paste.

Если репозитория нет под рукой — можно развернуть из zip:

```bash
pkg install -y unzip
unzip fomo-timer-bot.zip
cd fomo-timer-bot
bash install.sh
```

Запуск бота: `bash start.sh`. Он сам подтягивает обновления из GitHub
(если есть сеть и репо — правки уйдут в stash, личные .env и data/ не
трогаются), печатает версию, берёт wake-lock и сам перезапускает бота
при падении. Остановка: Ctrl+C (кнопка Ctrl в дополнительной строке
клавиатуры Termux, или зажми «Громкость вниз» и нажми C).

> **Почему `bash install.sh`, а не `./install.sh`?** Git при пуше с Windows
> не сохраняет бит «исполняемый файл» — после `git clone` на андроиде
> `./install.sh` отвечал бы «Permission denied». Запуск через `bash` работает
> всегда. Права можно вернуть и руками: `chmod +x *.sh` (install.sh делает
> это сам при каждом запуске).

---

## Перенос данных с ПК через свой репозиторий (5 минут)

`.env` и `data/` в git не попадают по задумке — там токен и личные данные,
.gitignore их бережёт. Поэтому в свой репозиторий их «проталкивают» один
раз принудительно, а телефон забирает обычным `git pull`.

**Шаг 0. Репозиторий должен быть ПРИВАТНЫМ** (иначе токен увидит весь
интернет). Открой https://github.com/jadmag007/fomo-timer-bot →
**Settings → General**, прокрути в самый низ до **Danger Zone** →
**Change repository visibility** → **Make private** → подтверди, введя
имя репозитория.

**Шаг 1. Дай телефону право читать приватное** (один раз). На GitHub:
аватар справа вверху → **Settings → Developer settings → Personal access
tokens → Tokens (classic) → Generate new token (classic)**: Note —
`fomo-phone`, срок любой (можно No expiration), галочка **repo** →
**Generate token**. Скопируй токен (начинается с `ghp_`) — его показывают
один раз, он понадобится в шаге 3.

**Шаг 2. С ПК отправь данные.** Останови бота (Ctrl+C в его окне), открой
cmd:

```
cd F:\FOMO\fomo-timer-bot
git add -f .env data
git rm --cached --ignore-unmatch "data/*.session" "data/*.session-journal" "data/*.log"
git commit -m "sync data pc to phone"
git push
```

`-f` проталкивает игнорируемое; строка с `rm --cached` вынимает из посылки
сессию Telegram и логи (сами файлы на ПК остаются — в git им не место).

**Шаг 3. На телефоне забери** (Termux):

```bash
cd ~/fomo-timer-bot
git config core.fileMode false
git config credential.helper store
git pull
```

Спросит Username: `jadmag007`, Password: длинный тап по экрану → Paste →
вставь токен из шага 1 (символы не отображаются — это норма). Готово:
`.env` и `data/` лежат в папке бота.

⚠️ Сессию юзербота (`userbot.session` — вход в твой Telegram) через git не
возим намеренно: на телефоне она не нужна, а если понадобится — там же
выполни `bash login_bot.sh`.

**Дальше данные обновляются так:** останови бота на обеих сторонах →
на той, где свежие данные, повтори шаг 2 → на другой стороне `git pull` →
запусти бота. Правило «один бот = один запущенный экземпляр» действует
как всегда.

Остальное поставит `install.sh`. После переноса проверь: `bash start.sh` →
в консоли появится список загруженных напоминаний и `FOMO auth ok`.

## ⚠️ Главное правило: один бот = один запущенный экземпляр

Если бот запущен **и на ПК, и на телефоне одновременно**, Telegram начнёт
выдавать `409 Conflict: terminated by other getUpdates request` — пушами
будет шуметь то один, то другой. Включи бота только на чём-то одном.
Второй экземпляр как «холодный резерв» — нормально, просто не держи оба
запущенными.

---

## Чтобы Android не убил бота

Android агрессивно замораживает фоновые процессы. Три шага против этого:

1. **Wake-lock** — `start.sh` берёт его сам (строка `termux-wake-lock`),
   ничего делать не надо. Уведомление «Termux: acquiring wakelock» — норма.
2. **Батарея без ограничений**: Настройки → Приложения → Termux →
   Батарея → «Без ограничений» (на некоторых прошивках — «Не оптимизировать»).
3. **Не свайпай Termux** из списка недавних приложений — это убивает процесс.

При перезагрузке телефона бот сам не стартует. Хочешь автозапуск —
поставь приложение **Termux:Boot** (тоже с F-Droid), открой его один раз,
затем:

```bash
mkdir -p ~/.termux/boot
cat > ~/.termux/boot/fomo.sh << 'EOF'
#!/data/data/com.termux/files/usr/bin/bash
termux-wake-lock
cd ~/fomo-timer-bot
bash start.sh
EOF
chmod +x ~/.termux/boot/fomo.sh
```

---

## Страница таймеров в браузере

Всё уже работает: страница слушает на этом же устройстве (только 127.0.0.1,
наружу не торчит).

1. Бот запущен (`bash start.sh`)
2. Открой в браузере телефона: **http://127.0.0.1:8080**
3. Или прямо из Termux: `termux-open http://127.0.0.1:8080`

В Chrome можно «Добавить на главный экран» — будет иконка на весь экран,
как приложение. Адрес порта (если 8080 занят) покажет `/апи` у бота.

---

## Обновление бота

Если ставил через git:

```bash
cd ~/fomo-timer-bot
git pull
bash start.sh
```

`.env` и `data/` git не трогает. Если ставил из zip — распакуй новый zip
поверх папки (кроме личных файлов, как при первом переносе) и перезапусти.

---

## Публикация обновлений с телефона (github_push.sh)

Это обратная операция: не «забрать обновления», а «опубликовать свои».
Понадобится, если ты правил код на телефоне и хочешь отправить правки
в свой репозиторий (например, чтобы второй телефон/ПК забрал их через
`git pull`).

1. Скачай `fomo-timer-bot.zip` в папку **Download** телефона.
2. В Termux:

```bash
cd ~/fomo-timer-bot
bash github_push.sh
```

Скрипт сам: найдёт СВЕЖИЙ из скачанных архивов (браузер называет
повторные скачивания «fomo-timer-bot (1).zip», «(2)»… — берётся самый
новый по дате), распакует во временную папку и снимет обёртку
`fomo-timer-bot/` (файлы лягут ровно в корень папки бота — личные `.env`,
`data/`, сессии не трогаются), вычистит мусор прошлых запусков (вложенные
копии, `*.part`), закоммитит, запушит и **удалит все скачанные zip из
Download** — куча одинаковых архивов не растёт. Git-репозиторий создастся
сам, а git-имя/почта подставятся из адреса репозитория
(`ник@users.noreply.github.com`) — отвечать «кто ты» не придётся.

Повторный запуск БЕЗ нового zip не ошибка: если пуш в прошлый раз упал
по сети, скрипт просто дожмёт уже закоммиченное (архив для этого не
нужен).

**Если репозитория в папке ещё нет** (полное восстановление с нуля):

```bash
mkdir -p ~/fomo-timer-bot
cp ~/storage/downloads/fomo-timer-bot.zip ~/fomo-timer-bot/
cd ~/fomo-timer-bot
python -m zipfile -e fomo-timer-bot.zip .
bash fomo-timer-bot/github_push.sh
```

Скрипт сам создаст репозиторий, подхватит историю с GitHub (релиз ляжет
одним чистым коммитом поверх — без force) и запушит.

**Токен для пуша (один раз):** добавь в `.env` строку
`GITHUB_TOKEN=ghp_…` (классический PAT с правом repo — как в разделе
переноса данных выше). Скрипт использует его через credential helper
и не показывает в логах. Альтернатива без токена:
`git config credential.helper store`, затем один раз `git pull`
(логин + токен как пароль) — дальше скрипт пушит сам.

**«TLS connect error … unexpected eof while reading»** — мобильная сеть
обрывает TLS-соединение с GitHub (частая история у российских операторов,
особенно на трафике). Скрипт сам делает 4 попытки с паузами и HTTP/1.1.
Если не пробилось — подожди 5–10 минут и запусти ещё раз (закоммиченное
не теряется, скрипт дожмёт тот же коммит) или включи VPN на минуту.

## Расход батареи

Wake-lock держит процессор активным, поэтому расход заметный. Реалистичная
оценка: 3–8% за ночь у бота в кармане. Если телефон живёт от зарядки или
это второй/старый телефон — вообще не проблема. Игра сама по себе всё равно
требует открытого Telegram на основном телефоне — боту хватает фонового
процесса, экран включать не надо.

---

## Troubleshooting

| Симптом | Причина и лечение |
|---------|-------------------|
| `error: Your local changes ... would be overwritten by merge` (git pull не проходит) | На телефоне остались ручные правки служебных файлов (например, install.sh). Из-за них git НЕ обновлял код — и ты запускал старый установщик. Просто запусти `bash install.sh` — новый установщик сам уведёт правки в stash и обновится. Вручную: `git stash && git pull` (вернуть правки: `git stash pop`) |
| pip снова пытается собрать `pydantic-core` (Failed to build / Rust not found) | Сначала `git pull` — установка могла запуститься старым установщиком (первая строка install.sh показывает его версию), затем снова `bash install.sh`. Если не помогло — выключи или смени VPN: зеркало TUR (`https://termux-user-repository.github.io/pypi/`) живёт на github.io. Запасная сборка: `pkg install -y rust binutils`, затем снова `bash install.sh` (rustup НЕ подходит — он не умеет андроид-таргет) |
| Установка падала, а теперь «та же ошибка» на новом установщике | Проверь баннер в первой строке install.sh: если там не самая свежая версия — телефон запускает старый скрипт: `cd ~/fomo-timer-bot && git pull` и снова `bash install.sh` |
| `./install.sh`: Permission denied | Git с Windows не хранит exec-бит: запускай `bash install.sh` и `bash start.sh` — работает всегда (или один раз `chmod +x *.sh`) |
| Бот «засыпает» через пару часов | Оптимизация батареи: см. раздел выше (пункт 2) |
| `409 Conflict` в логе | Где-то запущен второй экземпляр с тем же токеном — останови на ПК или на телефоне |
| `git push` падает с `TLS connect error … unexpected eof while reading` | Мобильная сеть рвёт TLS до GitHub. Скрипт github_push.sh сам ретраит (4 попытки, HTTP/1.1); если не пробилось — подожди 5–10 минут и запусти снова или включи VPN на минуту. Коммит не теряется |
| `Please tell me who you are` при коммите | Больше не должно случаться: github_push.sh сам ставит имя/почту из адреса репозитория. Если всё же — `git config --global user.name "Ник"` и `git config --global user.email "ник@users.noreply.github.com"` |
| `pip` ругается при установке | см. первые две строки таблицы; затем снова `bash install.sh` |
| Частые сетевые обрывы в логе | Мобильный интернет; бот перезапрашивает сам, это не ошибка |
| Termux просит хранилище | Не нужно: боту хватает своей папки в домашнем каталоге Termux |
